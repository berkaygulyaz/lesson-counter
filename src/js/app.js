window.addEventListener('load', () => {
    const band = document.querySelector('.yellow-band');
    new stepChange;
    new userName;

    
    setTimeout(() => {
        band.classList.add('hidden')
    }, 200)
})