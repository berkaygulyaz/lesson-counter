window.addEventListener('load', () => {
    const band = document.querySelector('.yellow-band');
    new StepChange;
    new userName;
    new Stopwatch;
    
    setTimeout(() => {
        band.classList.add('hidden')
    }, 200)
})